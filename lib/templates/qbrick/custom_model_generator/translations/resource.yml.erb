<%= I18n.locale %>:
  cms:
    <%= plural_name %>:
      singular_name: TRANSLATEME
      plural_name: TRANSLATEME
      navigation_title: TRANSLATEME

  activerecord:
    attributes:
      <%= model_name %>:
        <% attributes.each do |attr|-%>
<%= "#{attr}: TRANSLATE ME" %>
<% end %>
    models:
      <%= model_name %>:
        one: TRANSLATEME
        other: TRANSLATEME
