<%= I18n.locale %>:
  cms:
    navigation_title:
      <%= model_name %>: TRANSLATEME
  activerecord:
    models:
      <%= model_name %>:
        one: TRANSLATEME
        other: TRNASLATEME
    attributes:
      <%= model_name -%>:
        <%- attributes.each do |attr| -%>
        <%= "#{attr}: TRANSLATE ME" %>
        <%- end -%>